// Auth/login feature has been removed. No auth tests needed.
import { describe, it } from "vitest";

describe("auth", () => {
  it("login feature removed — no auth tests", () => {
    // intentionally empty
  });
});
